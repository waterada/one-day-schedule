---
name: release
description: バージョンを 1 つ上げてデプロイし、ここまでの変更に対するコミットメッセージを提案する。「リリースして」と頼まれたときや /release が呼ばれたときに使う
---

# release

「リリースして」と頼まれたときの一連の作業をまとめる。バンプ種別の確認 → `public/version.js` 更新 → `npx wrangler deploy` → コミットメッセージ提案、までを順に行う。

## 手順

### 1. バンプ種別を判定して確認

`git status` と `git diff`（必要なら `git log` も）で直近コミットされていない変更を確認し、以下のルールで bump 種別を提案する:

- **バグ修正のみ** → patch（`x.y.Z+1`）。例: `1.3.4 → 1.3.5`
- **機能追加・改善・UI 変更** → minor（`x.Y+1.0`、Z は 0 にリセット）。例: `1.3.4 → 1.4.0`
- 判定が曖昧なときは、変更内容を要約したうえでユーザーにどちらか確認する

`public/version.js` の現バージョンを読み取り、提案する新バージョン番号を**ユーザーに提示して、これでよいか必ず確認する**。OK が出るまで先に進まない。

### 2. `public/version.js` を更新

確認が取れたら `public/version.js` の `APP_VERSION` を新しい値に書き換える。

```js
const APP_VERSION = 'X.Y.Z';
```

シングルクォート・末尾セミコロンの形式を維持する。

### 3. デプロイ

```bash
npx wrangler deploy
```

を実行する。失敗したら出力を添えて報告し、ここで止まる。

### 4. コミットメッセージを提案（コミットはしない）

デプロイ成功後、`git status` / `git diff` で全変更（バージョン更新を含む）を確認し、**日本語のコミットメッセージを 1 つ提案するだけで止まる**。

- 既存コミットの文体に合わせる: 短い 1〜数行の自然な日本語、Co-Authored-By 行なし
- コミットの実行はしない。ユーザーが内容を見て自分でコミットする
- ユーザーが「これでコミットして」と続けて言ったら、そのときに実行する
