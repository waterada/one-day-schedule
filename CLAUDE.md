# プロジェクト指示

## 利用者

8 歳の娘が使う。UI/UX の判断は子ども向けを前提にする（誤タップしにくい、わかりやすい、文字・タップ領域は大きめ、など）。

## リリース手順

「リリースして」と言われたら、以下を実行する:

1. `public/version.js` の `APP_VERSION` を上げる（PWA キャッシュ更新のため）
2. `npx wrangler deploy`
